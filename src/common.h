
// options for the current image
// probably needs refactoring in milestone 0.2
#typedef struct
{
	int mode,
} OPTIONS;

// settings for the current pen
#typedef struct
{
	int colour,
	int width,
} PEN;
